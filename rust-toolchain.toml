[toolchain]
channel = "1.98.0"
components = ["clippy", "rustfmt", "rust-src", "rust-analyzer"]
# Minimal base: install only what the project needs (no rust-docs etc.), keeping
# the toolchain download small; `components` above add precisely what we use.
profile = "minimal"
